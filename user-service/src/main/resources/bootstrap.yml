

spring:
  application:
    name: userservice
  profiles:
    active: dev #1.配置文件中间写的环境
  cloud:
    nacos:
      server-addr: localhost:81
      config:
        file-extension: yaml #2.这里配置nacos服务器中的配置文文件名称
#      discovery:
#        cluster-name: HZ
#        ephemeral: false
#        namespace: 命名空间号

